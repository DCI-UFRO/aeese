func_inicializado <- function(){
  #Definir los valores de los vectores para guardar la informacion 
  #Si se desean modificar los nombres de estas variables igual es necesario modificar los valores
  #de las funciones peso_corriente, peso_max y peso_min
  valorx  <<- vector("character",10);
  pg <<- vector("character",10);
  cg <<- vector("character",10);
  p_c <<- vector("character",10);
  #definir la posicion inicial en el vector para guardar los valores de la funcion
  i<<-1;
}